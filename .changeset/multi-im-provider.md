---
'@0xwelt/legion': minor
'@0xwelt/legion-api': patch
---

Support multiple simultaneous IM providers via `MultiIMProvider`. The gateway now collects all configured IM platforms instead of stopping at the first one, enabling Web UI to run alongside Discord/Lark.
